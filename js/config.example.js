/* ============================================================
   SKETCH.DUEL config template.
   Copy this file to js/config.js and paste your Gemini API key.
   js/config.js is gitignored, so the key stays on your machine.
   Get a free key: https://aistudio.google.com  >  Get API key
   ============================================================ */

window.SKETCHDUEL_CONFIG = {
  GEMINI_API_KEY: "PASTE-YOUR-KEY-HERE"
  // optional: override the model if your key lacks quota for the default
  // GEMINI_MODEL: "gemini-3.5-flash-lite"
};
